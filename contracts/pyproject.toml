[tool.poetry]
name = "contracts"
version = "0.1.0"
description = ""
authors = ["Simon Ingeson <44818+smonn@users.noreply.github.com>"]

[tool.poetry.dependencies]
python = "^3.8"
py-algorand-sdk = "^1.9.0"
pyteal = "^0.9.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
